name: CI
on:
  push:
    branches: [ master ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Setup Node
      uses: actions/setup-node@v1
    - name: Install
      run: npm install
    - name: Test
      run: npm test
    - name: Deploy
      uses: "actions/heroku@master"
      args: |
        "container:login"
        "container:push -a cleinly web"
        "container:release -a cleinly web"
      secrets: ["HEROKU_API_KEY"]